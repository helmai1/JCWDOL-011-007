/* 1. multiplication table of a given integer. */
const num = 9; //input num here!
for (let index = 1; index < num; index++) {
  let multiplication = num * index;
  console.log(`${num} x ${index} = ${multiplication}`);
}
